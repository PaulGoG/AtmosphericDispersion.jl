# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # To group all GitHub Actions updates into a single PR, uncomment the following:
    # groups:
    #   github-actions:
    #     patterns:
    #       - "*"
  - package-ecosystem: "julia"
    directories:
      - "/"
      - "/docs"
      - "/test"
    schedule:
      interval: "weekly"
    # Standard libraries are versioned with Julia itself, so a [compat] entry
    # on one is not a requirement that can be bumped.
    ignore:
      - dependency-name: "Test"
      - dependency-name: "TOML"
      - dependency-name: "Printf"
    # To group all Julia dependency updates into a single PR, uncomment the following:
    # groups:
    #   julia-dependencies:
    #     patterns:
    #       - "*"
